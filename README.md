# Python-Project-Management-CLI-Tool

Built with Python using object-oriented programming to organize users, projects, and tasks. Uses argparse for CLI commands, JSON for saving and loading data, and property setters to validate task status.